tb.constant('CONF', {
	appName: appNamePlaceholder, // will be replaced by package.json name when compiled
	debug: appDebugPlaceholder,	// will be true for when compiled for dev environment, false otherwise
});